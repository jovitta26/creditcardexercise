function creditCard() {

      var creditCard = prompt ("Ingrese numero de tarjeta")

             for ( var i= 0 ; i >=creditCard.length; i++)
                 if ( object[i].length > num)
              return creditCard;
}

           if (creditCard.length > 19)
      console.log("")

   function invertir (str) {
     var x = str.length;
     var stringInvertida="";

     while (x>=0) {
      stringInvertida = stringInvertida + string.charAt(x);
       x--
       return stringInvertida
     }
